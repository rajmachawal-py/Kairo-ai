[project]
name = "insights-backend"
version = "0.1.0"
description = "iNSIGHTS FastAPI backend"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
